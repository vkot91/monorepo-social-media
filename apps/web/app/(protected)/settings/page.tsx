import { SectionPlaceholder } from "#/components/layout/section-placeholder";

export default function SettingsPage() {
  return (
    <SectionPlaceholder
      description="Preferences, privacy options, and account controls will live here."
      title="Settings"
    />
  );
}
